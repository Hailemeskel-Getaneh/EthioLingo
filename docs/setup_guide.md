# Setup & Development Guide

## Prerequisites
- Node.js >= 18
- Expo CLI

## Execution Steps
1. Install dependencies: `npm install`
2. Run Backend: `cd Backend && npm start`
3. Run Frontend: `cd EthioLingoFront && npx expo start`
